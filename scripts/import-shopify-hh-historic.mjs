#!/usr/bin/env node
/**
 * HIL-108 — CANCELED.
 *
 * Pre-Zoho Shopify HH historic sales → Analytics is no longer needed.
 * Do not run this script. Zoho historic plants (HIL-99) remain the Analytics backfill.
 *
 * Kept in repo only as abandoned work reference; exit immediately.
 */
console.error(
  "HIL-108 canceled: do not import historic Shopify HH orders. Use Zoho historic import (HIL-99) only.",
);
process.exit(1);
