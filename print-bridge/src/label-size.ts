/** Hilda shop label size (AirPrint-confirmed on QL-820NWBc). */
export const LABEL_WIDTH_MM = 60;
export const LABEL_HEIGHT_MM = 86;

/** Default `lp -o` media for the AirPrint queue. */
export const DEFAULT_LP_MEDIA = `Custom.${LABEL_WIDTH_MM}x${LABEL_HEIGHT_MM}mm`;
